# Sample data

Synthetic portfolio fixtures — fake tickers, round-number quantities, obvious test values. They match the schema of the Trading 212 API responses so the app can be demonstrated without a live account or API key.

Real portfolio snapshots are produced at runtime from the T212 API and are excluded from version control. See the main [README](../../README.md) for setup.
